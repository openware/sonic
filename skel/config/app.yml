port: 6969

asset:
  init: yarn install
  build: yarn build
  copy: cp -r client/build/ public/assets/

database:
  driver: "mysql"
  host: localhost
  port: 3306
  user: root
  name: opendax_development

redis:
  host: localhost
  port: 6379
